[project]
name = "laya-playground-apple"
version = "0.1.0"
description = "The Laya playground's Lane Runner on laya-apple: MLX GPU vs Apple Neural Engine, measured"
readme = "README.md"
license = "MIT"
requires-python = ">=3.11,<3.14"
# laya-apple is pinned exactly: every recorded result names the runtime version it came from.
# The [ane] extra brings coremltools for the Neural Engine path.
dependencies = [
    "laya-apple[ane]==1.0.2",
]

[tool.uv]
package = false
